import { extendTheme } from "@chakra-ui/react";

// custom "react-hook-form" style theme
export const theme = extendTheme({
    colors: {
        brand: {
            pink: "#ec5990",
            background: "#191d3a",
            purple: "#0f111b",
        },
    },
});
